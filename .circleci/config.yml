version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run:
          command: python ./tests_chat_bot.py
          name: Project2 M3 Test
  environment:
      DATABASE_URL=postgresql://aaratis:srikumar@localhost/postgres
      
workflows:
  main:
    jobs:
      - build-and-test

